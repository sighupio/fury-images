---
kind: pipeline
name: test

platform:
  os: linux
  arch: amd64

steps:
  - name: integration-test
    image: docker:dind
    pull: always
    commands:
      - ./build.sh
    volumes:
      - name: dockersock
        path: /var/run/docker.sock
    when:
      event:
      - push

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock
